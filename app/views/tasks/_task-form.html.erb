<%= simple_form_for task do |f| %>
  <%= f.error_notification %>

  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.association :category, selected: "General" %>

  <%= f.button :submit, class: "btn btn-primary wide-btn" %>
<% end %>
